{
  "name": "draftloop",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "@mermaid-js/mermaid-cli": "^11.0.0",
    "typescript": "^5.6.2"
  }
}
